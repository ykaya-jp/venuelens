name: ci

on:
  pull_request:
  push:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_VERSION: "22"

jobs:
  # ──────────────────────────────────────
  # Lint — コードの書き方ルール違反を検出
  # ──────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - run: npm ci

      - run: npm run lint

  # ──────────────────────────────────────
  # Type Check — TypeScript の型エラーを検出
  # ──────────────────────────────────────
  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - run: npx tsc --noEmit

  # ──────────────────────────────────────
  # Build — 本番ビルドが通るか検証
  # ──────────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Build Next.js
        # Use build:local — skips `prisma migrate deploy` which needs a
        # reachable DB. Migrations run during Vercel production deploys, not
        # in CI. Build-time env stubs only feed `next build`'s static checks.
        run: npm run build:local
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder
          DATABASE_URL: postgresql://placeholder:placeholder@localhost:5432/placeholder

  # ──────────────────────────────────────
  # Prisma — スキーマの整合性チェック
  # ──────────────────────────────────────
  prisma:
    name: Prisma Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - run: npm ci

      - name: Validate schema
        run: npx prisma validate

  # ──────────────────────────────────────
  # Unit Test — Vitest ユニットテスト
  # ──────────────────────────────────────
  unit-test:
    name: Unit Test
    needs: [lint]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - run: npm test

  # ──────────────────────────────────────
  # Bundle Size Guard — 主要 chunk が膨れた瞬間に CI で気づく
  # (Phase 4 launch readiness で追加)
  # ──────────────────────────────────────
  bundle-size:
    name: Bundle size guard
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Build Next.js (no analyze)
        run: npm run build:local
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder
          DATABASE_URL: postgresql://placeholder:placeholder@localhost:5432/placeholder

      - name: Check largest chunk size
        # Soft warn at 450 KB (current baseline 435 KB + 3.5 % headroom),
        # hard fail at 500 KB (current + 15 %). Tightens as the bundle
        # shrinks — bump these values down once docs/harness/bundle-baseline-2.md
        # § 3 candidates land. Largest-chunk is a proxy for First Load JS;
        # see bundle-baseline-2.md § 4 for the rationale.
        run: |
          MAX=$(find .next/static/chunks -name '*.js' -printf '%s\n' | sort -nr | head -1)
          MAX_KB=$((MAX / 1024))
          echo "Largest chunk: ${MAX_KB} KB"
          if [ "$MAX_KB" -gt 500 ]; then
            echo "::error::Largest chunk ${MAX_KB} KB exceeds 500 KB ceiling — see docs/harness/bundle-baseline-2.md"
            exit 1
          fi
          if [ "$MAX_KB" -gt 450 ]; then
            echo "::warning::Largest chunk ${MAX_KB} KB approaching 500 KB ceiling"
          fi

  # ──────────────────────────────────────
  # E2E Test — Playwright ブラウザテス���
  # (テストファイルが存在する場合のみ実行)
  # ──────────────────────────────────────
  e2e-test:
    name: E2E Test
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Check for E2E tests
        id: check
        run: |
          if find tests/e2e -name '*.ts' -o -name '*.spec.ts' 2>/dev/null | grep -q .; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_tests=false" >> "$GITHUB_OUTPUT"
            echo "No E2E test files found — skipping"
          fi

      - uses: actions/setup-node@v6
        if: steps.check.outputs.has_tests == 'true'
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        if: steps.check.outputs.has_tests == 'true'
        run: npm ci

      - name: Generate Prisma Client
        if: steps.check.outputs.has_tests == 'true'
        run: npx prisma generate

      - name: Install Playwright browsers
        if: steps.check.outputs.has_tests == 'true'
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests (excluding @quarantine)
        if: steps.check.outputs.has_tests == 'true'
        # Phase 4/5 quarantine flow — flaky specs tagged with the
        # @quarantine annotation (`test("...", { tag: "@quarantine" }, async ...)`)
        # are excluded from the blocking job and run separately in
        # `e2e-quarantine` below. This keeps the PR-blocking job
        # green while flaky tests are being triaged. Quarantine
        # workflow is documented in docs/harness/runbook.md.
        run: npx playwright test --grep-invert "@quarantine"
        env:
          # Existing E2E specs cover unauthenticated flows only (landing,
          # signup form validation, pages that redirect to /login). They do
          # not need a real Supabase connection, so prefer GitHub secrets
          # when provided but fall back to the same placeholder pattern the
          # `build` job uses — this keeps CI green without requiring the
          # repository to carry production credentials.
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'placeholder-anon-key' }}
          DATABASE_URL: ${{ secrets.DATABASE_URL || 'postgresql://placeholder:placeholder@localhost:5432/placeholder' }}

      - name: Upload test report
        if: steps.check.outputs.has_tests == 'true' && always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14

  # ──────────────────────────────────────
  # E2E Quarantine — flaky test を隔離して観測する informational job
  # (Phase 4/5: continue-on-error で PR を block しない)
  # ──────────────────────────────────────
  e2e-quarantine:
    name: E2E Quarantine (informational)
    needs: [build]
    runs-on: ubuntu-latest
    # Hard-skip when the PR title says [skip-quarantine] — saves the
    # 3-4 min CI when no quarantined spec exists yet (current state).
    # Remove this guard once the first @quarantine tag lands.
    if: |
      !contains(github.event.pull_request.title, '[skip-quarantine]')
    steps:
      - uses: actions/checkout@v5

      - name: Check for quarantined E2E tests
        id: check
        # Look for `@quarantine` literal anywhere in tests/e2e/*.ts.
        # If absent, the job exits cleanly without spinning up node /
        # browser deps — saves ~3-4 min on every PR until a real
        # quarantined spec lands.
        run: |
          if grep -r '@quarantine' tests/e2e 2>/dev/null | grep -q .; then
            echo "has_quarantined=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_quarantined=false" >> "$GITHUB_OUTPUT"
            echo "No @quarantine tags found — skipping"
          fi

      - uses: actions/setup-node@v6
        if: steps.check.outputs.has_quarantined == 'true'
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        if: steps.check.outputs.has_quarantined == 'true'
        run: npm ci

      - name: Generate Prisma Client
        if: steps.check.outputs.has_quarantined == 'true'
        run: npx prisma generate

      - name: Install Playwright browsers
        if: steps.check.outputs.has_quarantined == 'true'
        run: npx playwright install --with-deps chromium

      - name: Run quarantined E2E tests (informational)
        if: steps.check.outputs.has_quarantined == 'true'
        # Critical: this step's failure NEVER blocks the PR. It exists
        # to surface "the quarantined specs are still unstable" as an
        # observable signal in the GitHub Checks tab. Triage the
        # underlying flake; remove the @quarantine tag when fixed.
        continue-on-error: true
        run: npx playwright test --grep "@quarantine"
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'placeholder-anon-key' }}
          DATABASE_URL: ${{ secrets.DATABASE_URL || 'postgresql://placeholder:placeholder@localhost:5432/placeholder' }}

      - name: Upload quarantine report
        if: steps.check.outputs.has_quarantined == 'true' && always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-quarantine-report
          path: playwright-report/
          retention-days: 14
